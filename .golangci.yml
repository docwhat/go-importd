# spell-checker: disable

service:
  golangci-lint-version: 1.36.x

run:
  tests: false

linters:
  enable-all: true
  disable:
    - funlen
    - gochecknoglobals
    - godox
    - lll
    - wsl
    - depguard

linters-settings:
  misspell:
    locale: US
    ignore-words:
      - importd
